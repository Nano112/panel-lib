# Project version (semver)
versionMajor=0
versionMinor=1
versionPatch=0

org.gradle.jvmargs=-Xmx4G
org.gradle.parallel=true
kotlin.code.style=official

# Fabric loader / Fabric Language Kotlin — what we BUILD against.
deps.fabric_loader=0.19.3
deps.flk=1.13.12+kotlin.2.4.0
# fabric.mod.json floors — minimum a user needs installed.
deps.fabric_loader_min=0.16.0
deps.flk_min=1.13.0

# Dear ImGui (imgui-java). 1.89.0 targets LWJGL 3.3.x, matching MC 1.21.x bundled LWJGL.
imgui_version=1.89.0
